# Database migrations

The initial schema is defined in `database/schema.py`. Add ordered migration files here when schema version 2 is introduced; migrations should be forward-only and safe to run more than once.
